# Decorators Spec
  Define the decorators used to indicate the tiles an advancement square covers.

## 1. Purpose
  Define a simple, flexible, scalable system that aligns with the book.

## 2. JSON (decorators.json)
  - Define decorator zone nesting (scales).
  - Define the pallet of colors.
  - Define the decorators by piece, type and zone colors.
  - Define the circular zones for src & dst tiles (toggled via mouse).

## 3. Code (decorators.js)
  Define


 ### 3.1 TODO
  Refactor demos so they don't need 'export' around the json fields.

## N. Invariants (typically last section)
  Formally redundant consequences of the spec that must always hold.
  Used as drift guards and cross-checks across data, code, and tests.

